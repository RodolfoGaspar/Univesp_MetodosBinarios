/* 
 * criar funções
 * int convertBinDec(char numStr[])
 * void convertDecBin(int numDec, char numBin[])
 */

#include <stdio.h>

int main(int n)
{  
  // char c = n;
  // int a = 0;

  // a = convertDecBin(c);

  printf("%i\n", n);

  return 0;
}

int convertDecBin(char num[]){
   printf("%d", num[0]);

   return 0;
 }
